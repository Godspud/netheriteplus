# custom_items

Minecraft Bedrock `behavior_packs` and `resource_packs` for custom items.

This repository was initialized and contains the existing project files.

## Next steps

- Add a remote: `git remote add origin <url>`
- Push: `git push -u origin main`